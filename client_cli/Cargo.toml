[package]
name = "client_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
client_core = { path = "../client_core" }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }